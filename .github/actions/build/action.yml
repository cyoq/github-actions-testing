name: 'Build'
description: 'Build action'

runs:
  using: "composite"
  steps: 
    - name: Print output
      run: echo Building of node application is starting..
      shell: powershell
    # - name: Install dependencies
    #   run : npm install
    #   shell: powershell